[package]
name = "mynet"
version = "0.1.0"
edition = "2024"

[dependencies]
shared_lib = { path = "../shared_lib" }
tokio = { version = "1", features = ["full"] }

serde = { version = "1", features = ["derive"] }